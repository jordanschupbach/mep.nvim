--- mep.nvim's own version — the single source of truth. Bump this table
--- when cutting a release; nothing else in the plugin should hardcode a
--- version number (e.g. mep.dashboard reads it to show "MEP vX.Y.Z"
--- alongside "NVIM vX.Y.Z", the same way `vim.version()` is the source of
--- truth for Neovim's own version).
return {
  major = 0,
  minor = 0,
  patch = 1,
}
